BOOKING.COM PRICE DROP ALERT

Hi <%= @user.email %>, great news! The following hotel prices have dropped:

<% @price_drops.each do |drop| %>
<% currency_symbol = drop[:currency] == "USD" ? "$" : "€" %>
<%= drop[:hotel_name] %> (<%= drop[:city_name] %>)
Dates: <%= drop[:check_in].strftime("%d %b") %> - <%= drop[:check_out].strftime("%d %b %Y") %>
Price: <%= currency_symbol %><%= number_with_precision(drop[:previous_price], precision: 2) %> → <%= currency_symbol %><%= number_with_precision(drop[:current_price], precision: 2) %>
Savings: <%= currency_symbol %><%= number_with_precision(drop[:savings], precision: 2) %>

<% end %>
---
You receive alerts when prices drop by more than <%= @user.currency == "USD" ? "$" : "€" %><%= number_with_precision(@user.price_notification_threshold, precision: 2) %>
Tickets@Petrucho.me
